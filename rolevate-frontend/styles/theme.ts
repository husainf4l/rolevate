/**
 * ROLEVATE AI Theme Configuration
 * Modern, fintech-inspired color palette optimized for Middle Eastern professional tastes
 */

export const theme = {
  colors: {
    // Base colors
    background: "#0F172A", // Deep navy (cool, elegant dark base)
    primaryText: "#F8FAFC", // Almost white (clean on dark)
    secondaryText: "#94A3B8", // Soft gray-blue (subtext, paragraph)
    
    // Accent colors
    primaryAccent: "#00C6AD", // Teal green (innovation, Middle East vibes)
    hoverAccent: "#14B8A6", // Slightly lighter teal for buttons on hover
    
    // UI elements
    ctaButtonBg: "#1E293B", // Steel dark gray (button backgrounds)
    cardBg: "#1E293B", // Panels, sections (slightly raised elements)
    borders: "#334155", // Subtle dark borders
    
    // Status colors
    success: "#10B981", // Green for passed interviews / highlights
    error: "#EF4444", // Red for failed flags
  },

  // You can extend this with other theme properties like:
  // spacing, typography, breakpoints, etc.
};

export default theme;
